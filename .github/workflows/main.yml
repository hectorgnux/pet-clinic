name:  CI

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on:ubuntu-latest

    steps:
      -users:  actions/checkout@v3
      -name:  Run a multiline script
      run:  |
          echo "Hola Mundo"
